apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: reviews
  namespace: test-ns
spec:
  hosts:
  - reviews
  http:
  - route:
    - destination:
        host: reviews
      weight: 50
    - destination:
        host: reviews-canary
      weight: 50
